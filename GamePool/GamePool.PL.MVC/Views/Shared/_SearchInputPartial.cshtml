@model SearchParametersVM

@using (Html.BeginForm("Index", "Search", FormMethod.Get, new { @class = "navbar-form navbar-right" }))
{
    <div class="form-group">
        @Html.EditorFor(model => model.GameName, new { htmlAttributes = new { @class = "form-control search-input", placeholder = "Search..." } })
        <button type="submit" class="search-button"><span class="glyphicon glyphicon-search"></span></button>
    </div>
}
